extends components/layout

block title
  title 注册

block main
  main(ng-app="inputErrorsApp", ng-cloak)
    div.appctrl(ng-controller="AppCtrl", layout="column", layout-align="center center")
      md-content
        form(name="projectForm")
          md-input-container.md-block
            label Email
            input(required, type="email", name="clientEmail", ng-model="project.clientEmail", minlength="10", maxlength="50", ng-pattern="/^.+@.+\..+$/")
            div(ng-messages="projectForm.clientEmail.$error", role="alert")
              div(ng-message-exp="['required', 'minlength', 'maxlength', 'pattern']") Your email must be between 10 and 100 characters long and look like an e-mail address.
